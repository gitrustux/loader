[package]
name = "rustux-userspace"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
strip = true
opt-level = "z"
lto = true
codegen-units = 1
